<Window x:Class="Hello_World.Shop.ShopView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shop="clr-namespace:Hello_World.Shop"
        mc:Ignorable="d"
        Title="ShopView" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=shop:ShopViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="50"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--<ListView Grid.Row="1" ItemsSource="{Binding HelloWorldProducers}"  BorderThickness="0">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Image Source="" Height="32" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        <ListView Grid.Row="1" ItemsSource="{Binding listViews}"  BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Count}"/>
                    <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Prize}"/>
                    <GridViewColumn Header="HWpS" DisplayMemberBinding="{Binding HelloWorldPerSecond}"/>
                    <GridViewColumn Width="35" Header="Buy">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Buy" Command="{Binding OnBuyButtonClickCommand}" CommandParameter="{Binding HelloWorldProducers[0]}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
